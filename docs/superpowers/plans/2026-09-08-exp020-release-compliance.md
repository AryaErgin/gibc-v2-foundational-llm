# EXP-020 final compliance implementation plan

Scope: documentation and a new inference-only distribution; frozen artifacts and scientific code/config remain unchanged. No inference, generation, benchmark requests, package changes or Git publication.

1. Audit official rules, recorded source identity and primary-source licenses.
2. Test-first: exact CPU tensor serialization, state loading without forward execution, manifest tamper and overwrite refusal.
3. Export a fresh ignored WSL release directory; preserve source checkpoint and compare every tensor bitwise. Bundle exact model source, config, tokenizer, instructions and notices.
4. Add narrow README compute/setup/release sections and a compliance/license/provenance supplement. Preserve historical records.
5. Run benchmark-free release/submission tests, exact package verification, link/figure checks and diff check. Report manual publication/video/team gates.
