# Data Sources

EXP-001A will document exact public dataset identity, immutable revision, license, fields, deterministic selection, preprocessing, tokenizer-training partition, held-out validation split, and contamination screening before data materialization.
