# EXP-001

## Question

Can the approved control architecture be trained reproducibly from scratch with auditable data provenance and correct evaluation infrastructure?

## Status

EXP-001A infrastructure was validated. EXP-001B is the research-chat-requested readiness hardening stage: it preserves the original bounded evidence while adding lm-eval v0.4.9.1 causal tokenization/rolling semantics, full non-cycled stream preparation, checkpoint data-cursor continuity, all-public-split multiple-choice decontamination, and immutable provenance locks. Controlled values are in `configs/exp001.yaml`; the exact whole-batch realization is 3,052 updates x 32,768 prediction tokens = 100,007,936. Limited zero-shot lm-eval calls are integration-only, not official scores. The full training run remains unauthorized pending research-chat approval.
