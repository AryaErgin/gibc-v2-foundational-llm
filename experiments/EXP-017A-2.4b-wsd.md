# EXP-017A — Recipe-v3 + WSD at 2.4B tokens

Status: preregistered. Attempt 1 is `TECHNICAL_ABORT_THERMAL` with `NO
SCIENTIFIC RESULT`; it may not be resumed.

## Question

Does the promoted Recipe-v3 + WSD training recipe retain its internal
held-out-validation advantage at the established 2.4B-token horizon? This is
a horizon-scaling/final-model-phase branch, not an optimizer, architecture,
tokenizer, data-mixture, batch, or validation intervention.

## Frozen branch

EXP-017A is a fresh seed-42, step-0 WSL/Linux lineage. It must not initialize
or resume from SYS-002, either EXP-016 arm, an EXP-013 stable checkpoint, or
any other trained weight. It uses Recipe-v3 (49,860,480 parameters), BF16,
AdamW beta1=0.9/beta2=0.95/eps=1e-8, matrix weight decay 0.1, gradient clip
1.0, context 512, physical batch 32, gradient accumulation 2, and the
immutable EXP-012 2.4B stream. There are exactly 73,242 optimizer updates,
64 windows per update, and 2,399,993,856 prediction tokens.

The immutable EXP-012 stream is SHA-256
`27c1c8d06da579d443ee19017e12dd28a7c3fb8c6387cff76e9128c7c5fba82c`;
its manifest is SHA-256
`b19b508dd1d1928b8e3bbdf586547791dc3bd76af19f6e55b8c39465bd749ccf`.
Its frozen tokenizer SHA-256 is
`c5592fba176c3d2f7915a3812559a24d7a669206f4a22484b053c8a9ce08be14`.
The exact stream prefix/provenance chain and both frozen validation tensors
must be verified before launch. No data rebuild is authorized.

## WSD horizon semantics and stable trunk

The scheduler implementation is authoritative. With 73,242 total updates,
100 warmup updates, and a rounded 10% cooldown of 7,324 updates, it produces:

| Quantity | Frozen value |
|---|---:|
| last stable update | 65,918 |
| first cooldown update | 65,919 |
| cooldown updates | 7,324 |
| stable cursor | 4,218,752 windows |
| stable prediction-token cursor | 2,160,001,024 |
| final update | 73,242 |
| final LR | 6e-5 |

The runner must save `checkpoint-step-65918.pt` at the pre-cooldown stable
boundary. It is a critical artifact and must contain model, optimizer, WSD
state, RNG state, data cursor, and source/config provenance. The cooldown
branch is validated at the terminal endpoint; it is never used to continue
the stable trunk.

## Internal reference and decision

The only reference is the exact internal held-out result from the authoritative
EXP-012 terminal artifact: General NLL `3.190959542989731`, Edu NLL
`2.8499483168125153`, combined NLL `3.020453929901123`. Its cosine schedule
is not an EXP-017 candidate or a benchmark-selection input.

Before results exist, WSD scaling SUCCESS requires combined held-out NLL at
least 0.010 below the exact EXP-012 cosine reference and neither General nor
Edu worse by more than 0.020. If the combined difference remains within
plus/minus 0.010 without the SUCCESS improvement, classify SCALE_TIE; WSD
remains promoted based on EXP-013 while horizon implications are reviewed.
If combined NLL is more than 0.010 worse, classify SCALE_REGRESSION and stop
horizon extension for diagnosis. These thresholds are frozen and will not be
reinterpreted after results.

The branch will record General, Edu, combined, descriptive PPL, runtime,
throughput, VRAM, GPU temperature/power, the stable-trunk checkpoint SHA-256,
and terminal checkpoint SHA-256. No official benchmark task may be invoked or
inspected for EXP-017A model selection.

## Future ladder (specification only)

If EXP-017A is a scaling SUCCESS, retain its pre-cooldown stable checkpoint
and later extend that stable lineage toward 4.8B rather than restarting from
zero or continuing a cooled-down branch. Future, separately authorized exact
horizons are 4.8B/146,484 updates/4,799,987,712 tokens;
7.2B/219,726 updates/7,199,981,568 tokens; and
9.6B/292,968 updates/9,599,975,424 tokens. This document neither builds the
additional data nor authorizes any of those runs.

## Safety

Actual GPU work requires OMEN Performance mode, single-threaded CPU-library
limits, no concurrent CPU-heavy task, finite loss, no CUDA/runtime anomaly,
and GPU temperature below 85 C. This preregistration does not launch training.

## Attempt 1 and unattended thermal gate

Attempt 1 began fresh from step 0 and was gracefully terminated on 2026-08-30
after sustained user-observed CPU package temperature near 95 C. Its logs,
GPU telemetry, supervisor status, and
`attempt-1-thermal-abort.json` are preserved; it yielded no terminal
checkpoint, validation, or scientific result. Do not use its partial metrics.

The original runner buffered per-step records until a long chunk returned. A
separate fsynced, output-only `progress.jsonl` now records completed-update
step, tokens, cursor, loss, LR, timestamp, and throughput at most every 100
updates. It was tested against a tiny deterministic control without changing
model/optimizer/scheduler/RNG/checkpoint state. The existing system does not
expose a trustworthy identified CPU-package temperature source: the available
Windows `_tz.tz00` ACPI zone is not sufficient and WSL exposes no hwmon source.
`UNATTENDED_EXP017A_ALLOWED = NO`. A future fresh Attempt 2 requires a
trustworthy existing read-only CPU-package source, tested automatic CPU/GPU
abort behavior, a passing cooled shakedown, and separate authorization.
